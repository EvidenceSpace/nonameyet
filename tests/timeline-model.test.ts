import assert from"node:assert/strict";import test from"node:test";import{createTimelineEvent,sortTimelineEvents,validateTimelineEvent}from"../web/timeline-model.js";const hash="a".repeat(64),file={id:"file_1",sha256:hash};test("timeline event is user-entered and bound to exact source hash",()=>{const result=createTimelineEvent("case_1",{eventDate:"2026-07-01",title:" Files delivered ",description:" Sent final archive ",sourceFileId:"file_1"},file,"2026-07-02T00:00:00Z","event_1");assert.equal(result.ok,true);if(!result.ok)return;assert.equal(result.value.title,"Files delivered");assert.equal(result.value.origin,"user_entered");assert.equal(result.value.sourceReference.sha256,hash)});test("timeline validation rejects impossible dates bad fields and missing source",()=>{assert.equal(validateTimelineEvent({eventDate:"2026-02-30",title:"Delivered",sourceFileId:"file_1"}).ok,false);assert.equal(validateTimelineEvent({eventDate:"2026-02-20",title:"x",sourceFileId:"file_1"}).ok,false);assert.equal(validateTimelineEvent({eventDate:"2026-02-20",title:"Delivered",sourceFileId:""}).ok,false);assert.equal(createTimelineEvent("case_1",{eventDate:"2026-02-20",title:"Delivered",sourceFileId:"file_1"},{id:"file_1",sha256:"bad"},"now","event_1").ok,false)});test("timeline sorts by event date without mutating input",()=>{const input=[{id:"b",eventDate:"2026-07-02",createdAt:"2"},{id:"a",eventDate:"2026-07-01",createdAt:"1"}];assert.deepEqual(sortTimelineEvents(input).map(x=>x.id),["a","b"]);assert.equal(input[0].id,"b")});
