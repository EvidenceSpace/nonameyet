import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";test("intake privacy claims match implemented V1 behavior",async()=>{const html=await readFile(new URL("../web/cases-new.html",import.meta.url),"utf8");assert.ok(!html.includes("Delete my trial files automatically"));assert.ok(!html.includes("scheduled for deletion after 7 days"));assert.ok(!html.includes("tailored checklist"));assert.ok(html.includes("No automatic deletion schedule"));assert.ok(html.includes('id="local-storage-ack"'));assert.ok(html.includes("AI analysis requires a separate decision"))});
