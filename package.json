{
  "name": "casefind",
  "version": "0.1.0",
  "private": true,
  "description": "Source-linked case organization for unpaid freelance disputes.",
  "type": "module",
  "scripts": {
    "preview": "python3 -m http.server 4173 -d web",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test tests/**/*.test.ts",
    "check": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
