const DB_NAME="casefind-preview",DB_VERSION=4;function requestResult(request){return new Promise((resolve,reject)=>{request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error)})}export function openDatabase(){return new Promise((resolve,reject)=>{const request=indexedDB.open(DB_NAME,DB_VERSION);request.onupgradeneeded=()=>{const db=request.result;if(!db.objectStoreNames.contains("cases"))db.createObjectStore("cases",{keyPath:"id"});if(!db.objectStoreNames.contains("files")){const store=db.createObjectStore("files",{keyPath:"id"});store.createIndex("caseId","caseId",{unique:false});store.createIndex("caseHash",["caseId","sha256"],{unique:true})}if(!db.objectStoreNames.contains("facts")){const store=db.createObjectStore("facts",{keyPath:"id"});store.createIndex("caseId","caseId",{unique:false});store.createIndex("sourceFileId","sourceFileId",{unique:false})}if(!db.objectStoreNames.contains("suggestions")){const store=db.createObjectStore("suggestions",{keyPath:"id"});store.createIndex("caseId","caseId",{unique:false});store.createIndex("fileId","fileId",{unique:false})}if(!db.objectStoreNames.contains("processing")){const store=db.createObjectStore("processing",{keyPath:"fileId"});store.createIndex("caseId","caseId",{unique:false})}};request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error)})}async function transaction(storeName,mode,action){const db=await openDatabase();try{const tx=db.transaction(storeName,mode),result=await action(tx.objectStore(storeName));await new Promise((resolve,reject)=>{tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);tx.onabort=()=>reject(tx.error||new Error("Transaction aborted"))});return result}finally{db.close()}}export const saveCase=value=>transaction("cases","readwrite",store=>requestResult(store.put(value))),getCase=id=>transaction("cases","readonly",store=>requestResult(store.get(id))),getAllCases=()=>transaction("cases","readonly",store=>requestResult(store.getAll()));async function touchCase(caseId,at=new Date().toISOString()){if(!caseId)return;const record=await getCase(caseId);if(record)await saveCase({...record,updatedAt:at})}async function saveWithActivity(storeName,value,method){const result=await transaction(storeName,"readwrite",store=>requestResult(store[method](value)));await touchCase(value.caseId);return result}async function deleteWithActivity(storeName,id){const value=await transaction(storeName,"readonly",store=>requestResult(store.get(id)));await transaction(storeName,"readwrite",store=>requestResult(store.delete(id)));await touchCase(value?.caseId)}export const saveFile=value=>saveWithActivity("files",value,"add");export async function deleteFile(id){await deleteProcessing(id);return deleteWithActivity("files",id)}export const getFilesForCase=id=>transaction("files","readonly",store=>requestResult(store.index("caseId").getAll(id))),saveFact=value=>saveWithActivity("facts",value,"put"),getFactsForCase=id=>transaction("facts","readonly",store=>requestResult(store.index("caseId").getAll(id))),deleteFact=id=>deleteWithActivity("facts",id),saveSuggestion=value=>saveWithActivity("suggestions",value,"put"),getSuggestionsForCase=id=>transaction("suggestions","readonly",store=>requestResult(store.index("caseId").getAll(id))),deleteSuggestion=id=>deleteWithActivity("suggestions",id),saveProcessing=value=>saveWithActivity("processing",value,"put"),getProcessingForCase=id=>transaction("processing","readonly",store=>requestResult(store.index("caseId").getAll(id))),deleteProcessing=id=>deleteWithActivity("processing",id);export async function markCaseBackedUp(caseId,at=new Date().toISOString()){const record=await getCase(caseId);if(!record)throw new Error("Case not found.");await saveCase({...record,lastBackupAt:at})}export async function deleteCase(caseId){const groups=await Promise.all([getFilesForCase(caseId),getFactsForCase(caseId),getSuggestionsForCase(caseId),getProcessingForCase(caseId)]),stores=["files","facts","suggestions","processing"];for(let index=0;index<stores.length;index++){const storeName=stores[index];await transaction(storeName,"readwrite",async store=>{for(const value of groups[index])store.delete(storeName==="processing"?value.fileId:value.id)})}await transaction("cases","readwrite",store=>requestResult(store.delete(caseId)))}export async function restoreCaseBundle(bundle){const existing=await getCase(bundle.record.id);if(existing)throw new Error("This case already exists on this device. Nothing was overwritten.");const db=await openDatabase();try{const tx=db.transaction(["cases","files","facts","suggestions","processing"],"readwrite");tx.objectStore("cases").add(bundle.record);for(const file of bundle.files)tx.objectStore("files").add(file);for(const fact of bundle.facts)tx.objectStore("facts").add(fact);for(const suggestion of bundle.suggestions)tx.objectStore("suggestions").add(suggestion);for(const job of bundle.processing)tx.objectStore("processing").add(job);await new Promise((resolve,reject)=>{tx.oncomplete=resolve;tx.onerror=()=>reject(new Error("The backup collided with existing local data. Nothing was restored."));tx.onabort=()=>reject(new Error("The backup could not be restored. Nothing was changed."))});return bundle.record.id}finally{db.close()}}export async function sha256(file){const digest=await crypto.subtle.digest("SHA-256",await file.arrayBuffer());return[...new Uint8Array(digest)].map(byte=>byte.toString(16).padStart(2,"0")).join("")}export const createId=prefix=>`${prefix}_${crypto.randomUUID()}`;if(location.pathname.endsWith("/case.html")){import("./processing-ui.js");import("./report-ui.js");import("./backup-ui.js");import("./library-link.js")}
