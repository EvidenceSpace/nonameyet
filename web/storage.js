const DB_NAME = "casefind-preview";
const DB_VERSION = 4;
function requestResult(request) { return new Promise((resolve, reject) => { request.onsuccess = () => resolve(request.result); request.onerror = () => reject(request.error); }); }
export function openDatabase() { return new Promise((resolve, reject) => { const request = indexedDB.open(DB_NAME, DB_VERSION); request.onupgradeneeded = () => { const db = request.result; if (!db.objectStoreNames.contains("cases")) db.createObjectStore("cases", { keyPath: "id" }); if (!db.objectStoreNames.contains("files")) { const store = db.createObjectStore("files", { keyPath: "id" }); store.createIndex("caseId", "caseId", { unique: false }); store.createIndex("caseHash", ["caseId", "sha256"], { unique: true }); } if (!db.objectStoreNames.contains("facts")) { const store = db.createObjectStore("facts", { keyPath: "id" }); store.createIndex("caseId", "caseId", { unique: false }); store.createIndex("sourceFileId", "sourceFileId", { unique: false }); } if (!db.objectStoreNames.contains("suggestions")) { const store = db.createObjectStore("suggestions", { keyPath: "id" }); store.createIndex("caseId", "caseId", { unique: false }); store.createIndex("fileId", "fileId", { unique: false }); } if (!db.objectStoreNames.contains("processing")) { const store = db.createObjectStore("processing", { keyPath: "fileId" }); store.createIndex("caseId", "caseId", { unique: false }); } }; request.onsuccess = () => resolve(request.result); request.onerror = () => reject(request.error); }); }
async function transaction(storeName, mode, action) { const db = await openDatabase(); try { const tx = db.transaction(storeName, mode); const result = await action(tx.objectStore(storeName)); await new Promise((resolve, reject) => { tx.oncomplete = resolve; tx.onerror = () => reject(tx.error); tx.onabort = () => reject(tx.error || new Error("Transaction aborted")); }); return result; } finally { db.close(); } }
export const saveCase=value=>transaction("cases","readwrite",store=>requestResult(store.put(value)));export const getCase=id=>transaction("cases","readonly",store=>requestResult(store.get(id)));export const saveFile=value=>transaction("files","readwrite",store=>requestResult(store.add(value)));export async function deleteFile(id){await deleteProcessing(id);return transaction("files","readwrite",store=>requestResult(store.delete(id)))}export const getFilesForCase=id=>transaction("files","readonly",store=>requestResult(store.index("caseId").getAll(id)));export const saveFact=value=>transaction("facts","readwrite",store=>requestResult(store.put(value)));export const getFactsForCase=id=>transaction("facts","readonly",store=>requestResult(store.index("caseId").getAll(id)));export const deleteFact=id=>transaction("facts","readwrite",store=>requestResult(store.delete(id));export const saveSuggestion=value=>transaction("suggestions","readwrite",store=>requestResult(store.put(value)));export const getSuggestionsForCase=id=>transaction("suggestions","readonly",store=>requestResult(store.index("caseId").getAll(id)));export const deleteSuggestion=id=>transaction("suggestions","readwrite",store=>requestResult(store.delete(id));export const saveProcessing=value=>transaction("processing","readwrite",store=>requestResult(store.put(value)));export const getProcessingForCase=id=>transaction("processing","readonly",store=>requestResult(store.index("caseId").getAll(id)));export const deleteProcessing=id=>transaction("processing","readwrite",store=>requestResult(store.delete(id));
export async function deleteCase(caseId){const groups=await Promise.all([getFilesForCase(caseId),getFactsForCase(caseId),getSuggestionsForCase(caseId),getProcessingForCase(caseId)]),stores=["files","facts","suggestions","processing"];for(let index=0;index<stores.length;index++){const storeName=stores[index];await transaction(storeName,"readwrite",async store=>{for(const value of groups[index])store.delete(storeName==="processing"?value.fileId:value.id)})}await transaction("cases","readwrite",store=>requestResult(store.delete(caseId)))}
export async function sha256(file){const digest=await crypto.subtle.digest("SHA-256",await file.arrayBuffer());return[...new Uint8Array(digest)].map(byte=>byte.toString(16).padStart(2,"0")).join("")}export const createId=prefix=>`${prefix}_${crypto.randomUUID()}`;if(location.pathname.endsWith("/case.html")){import("./processing-ui.js");import("./report-ui.js");import("./backup-ui.js")}
