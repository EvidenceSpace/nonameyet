import{deleteCase,getCase,getFactsForCase,getFilesForCase,getProcessingForCase,getSuggestionsForCase,getEventsForCase}from"./storage.js";import{backupFreshness,canConfirmCaseDeletion,formatLocalBytes}from"./case-library-model.js";const caseId=new URLSearchParams(location.search).get("id");setTimeout(async()=>{const dialog=document.querySelector("#delete-dialog");if(!caseId||!dialog)return;const[record,files,facts,suggestions,processing,events]=await Promise.all([getCase(caseId),getFilesForCase(caseId),getFactsForCase(caseId),getSuggestionsForCase(caseId),getProcessingForCase(caseId),getEventsForCase(caseId)]);if(!record)return;const title=record.title||"Untitled case",backup=backupFreshness(record),bytes=files.reduce((sum,file)=>sum+(Number(file.size)||0),0);dialog.className="workspace-delete-dialog";dialog.dataset.enhanced="true";dialog.innerHTML=`<form id="workspace-delete-form"><div class="workspace-delete-head"><div><span class="section-label">PERMANENT LOCAL DELETION</span><h2>Delete “${escapeHtml(title)}”?</h2></div><button class="dialog-close" type="button" aria-label="Close">×</button></div><p>This removes this case and every related local record in one rollback-safe browser-storage operation.</p><div class="workspace-delete-summary"><div><strong>${files.length}</strong><span>original records</span></div><div><strong>${facts.length}</strong><span>facts</span></div><div><strong>${events.length}</strong><span>timeline events</span></div><div><strong>${formatLocalBytes(bytes)}</strong><span>original bytes</span></div></div><div class="workspace-delete-backup ${backup.state}">${backup.state==="current"?"A current encrypted backup is recorded on this device. Confirm you still have the downloaded file and password.":backup.state==="stale"?"The recorded encrypted backup is out of date. Recent changes may not be recoverable.":"No encrypted backup is recorded. Deletion will not be recoverable through CaseFind."}</div><label>Type the exact case title<input id="workspace-delete-title" autocomplete="off" spellcheck="false"></label><label class="workspace-delete-ack"><input id="workspace-delete-ack" type="checkbox"><span>I understand this permanently removes the case, ${files.length} original record${files.length===1?"":"s"}, ${facts.length} fact${facts.length===1?"":"s"}, ${suggestions.length} review item${suggestions.length===1?"":"s"}, ${events.length} timeline event${events.length===1?"":"s"}, and ${processing.length} processing result${processing.length===1?"":"s"} from this browser.</span></label><p class="workspace-delete-error" role="status"></p><div class="workspace-delete-actions"><button class="button-secondary workspace-delete-cancel" type="button">Cancel</button><button class="danger-button" id="workspace-confirm-delete" type="submit" disabled>Delete permanently</button></div></form>`;const form=dialog.querySelector("form"),input=dialog.querySelector("#workspace-delete-title"),ack=dialog.querySelector("#workspace-delete-ack"),confirm=dialog.querySelector("#workspace-confirm-delete"),error=dialog.querySelector(".workspace-delete-error");function validate(){confirm.disabled=!canConfirmCaseDeletion(title,input.value,ack.checked)}function close(){dialog.close();form.reset();error.textContent="";validate()}input.oninput=validate;ack.onchange=validate;dialog.querySelector(".dialog-close").onclick=close;dialog.querySelector(".workspace-delete-cancel").onclick=close;dialog.addEventListener("click",event=>{if(event.target===dialog)close()});form.onsubmit=async event=>{event.preventDefault();validate();if(confirm.disabled)return;confirm.disabled=true;confirm.textContent="Deleting locally…";error.textContent="";try{await deleteCase(caseId);location.href="cases.html"}catch(cause){error.textContent=cause instanceof Error?cause.message:"The case could not be deleted. Nothing was changed.";confirm.textContent="Delete permanently";validate()}};document.querySelector("#delete-case").addEventListener("click",()=>requestAnimationFrame(()=>input.focus()))},0);function escapeHtml(value){const node=document.createElement("span");node.textContent=value;return node.innerHTML}
