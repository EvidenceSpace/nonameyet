import{loadAccount,signOut}from"./account-client.js";
const mount=document.querySelector(".workspace-header-actions")||document.querySelector(".site-header nav");
if(mount){const trigger=document.createElement("button");trigger.type="button";trigger.className="account-trigger";trigger.textContent="Account";mount.prepend(trigger);const dialog=document.createElement("dialog");dialog.className="account-dialog";dialog.innerHTML=`<div class="account-dialog-head"><div><span class="section-label">ACCOUNT & PRIVACY</span><h2>Your cases stay on this device</h2></div><button class="dialog-close" type="button" aria-label="Close">×</button></div><div class="account-status" id="account-status"><span class="account-status-dot"></span><div><strong>Checking account…</strong><small></small></div></div><div class="account-privacy-panel"><strong>Signing in does not upload your cases.</strong><ul><li><span>✓</span><div><b>Cases remain in this browser</b><small>Case details, files, extracted text, facts, and suggestions are not synchronized.</small></div></li><li><span>✓</span><div><b>Sync stays off</b><small>There is no automatic case sync or cloud backup.</small></div></li><li><span>✓</span><div><b>Only identity metadata is saved</b><small>Your verified email, optional display name, and account-scoped usage are stored.</small></div></li></ul></div><div class="account-actions"><a class="button" id="account-sign-in" href="/api/auth/start" hidden>Sign in securely</a><button class="button-secondary" id="account-sign-out" type="button" hidden>Sign out</button><p id="account-message"></p></div>`;document.body.append(dialog);const close=dialog.querySelector(".dialog-close"),status=dialog.querySelector("#account-status"),signIn=dialog.querySelector("#account-sign-in"),signOutButton=dialog.querySelector("#account-sign-out"),message=dialog.querySelector("#account-message");close.onclick=()=>dialog.close();trigger.onclick=()=>dialog.showModal();dialog.addEventListener("click",event=>{if(event.target===dialog)dialog.close()});loadAccount().then(account=>{const strong=status.querySelector("strong"),small=status.querySelector("small");if(account.signedIn){status.classList.add("signed-in");strong.textContent=account.account.displayName||account.account.email;small.textContent=`${account.account.email} · Sync off`;trigger.textContent=account.account.displayName||"Account";signOutButton.hidden=false}else{strong.textContent="Not signed in";small.textContent=account.authenticationAvailable?"Sign in is optional. Local cases work without an account.":"Sign-in is not configured on this server.";signIn.hidden=!account.authenticationAvailable}}).catch(()=>{status.querySelector("strong").textContent="Account status unavailable";status.querySelector("small").textContent="Your local cases are unaffected."});signOutButton.onclick=async()=>{signOutButton.disabled=true;message.textContent="Signing out…";try{await signOut();location.reload()}catch{message.textContent="Could not sign out safely. Try again.";signOutButton.disabled=false}}}
