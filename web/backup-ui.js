import{getCase,getFilesForCase,getFactsForCase,getSuggestionsForCase,getProcessingForCase,markCaseBackedUp}from"./storage.js";import{createEncryptedCaseBackup,downloadEncryptedCaseBackup,backupLimits}from"./case-backup.js";const caseId=new URLSearchParams(location.search).get("id"),reportButton=document.querySelector(".workspace-topline .button-secondary");if(caseId&&reportButton){const button=document.createElement("button");button.className="button-secondary backup-trigger";button.type="button";button.textContent="Backup case";reportButton.before(button);const dialog=document.createElement("dialog");dialog.className="backup-dialog";dialog.innerHTML=`<form id="backup-form"><div class="backup-head"><div><span class="section-label">LOCAL RECOVERY</span><h2>Encrypt a complete backup</h2></div><button class="dialog-close" type="button" aria-label="Close">×</button></div><p class="backup-intro">Case details, facts, suggestions, processing results, and original record bytes will be encrypted in this browser before download.</p><div class="backup-summary" id="backup-summary"></div><label>Backup password <span>at least 12 characters</span><input id="backup-password" type="password" minlength="12" autocomplete="new-password" required></label><label>Confirm password<input id="backup-confirm" type="password" minlength="12" autocomplete="new-password" required></label><label class="backup-ack"><input id="backup-ack" type="checkbox"><span>I understand CaseFind cannot recover this password.</span></label><p class="backup-error" id="backup-error" role="status"></p><div class="backup-actions"><button class="button-secondary backup-cancel" type="button">Cancel</button><button class="button" id="create-backup" type="submit" disabled>Encrypt and download</button></div></form>`;document.body.append(dialog);const form=dialog.querySelector("form"),password=dialog.querySelector("#backup-password"),confirm=dialog.querySelector("#backup-confirm"),ack=dialog.querySelector("#backup-ack"),submit=dialog.querySelector("#create-backup"),error=dialog.querySelector("#backup-error");let record;function validate(){submit.disabled=password.value.length<12||password.value!==confirm.value||!ack.checked}password.oninput=confirm.oninput=ack.onchange=validate;function close(){dialog.close();form.reset();submit.disabled=true;error.textContent=""}dialog.querySelector(".dialog-close").onclick=close;dialog.querySelector(".backup-cancel").onclick=close;dialog.addEventListener("click",e=>{if(e.target===dialog)close()});button.onclick=async()=>{error.textContent="";const[loaded,files,facts,suggestions]=await Promise.all([getCase(caseId),getFilesForCase(caseId),getFactsForCase(caseId),getSuggestionsForCase(caseId)]);record=loaded;const size=files.reduce((n,f)=>n+(f.original?.size||0),0);dialog.querySelector("#backup-summary").innerHTML=`<div><strong>${files.length}</strong><span>original records</span></div><div><strong>${facts.length}</strong><span>facts</span></div><div><strong>${suggestions.length}</strong><span>suggestions</span></div><p>${(size/1024/1024).toFixed(1)} MB before encryption · ${backupLimits.iterations.toLocaleString()} password-hardening rounds</p>`;if(size>backupLimits.maxBytes)error.textContent="This case exceeds the 50 MB in-browser backup limit. Keep the original records separately.";dialog.showModal();password.focus()};form.onsubmit=async e=>{e.preventDefault();validate();if(submit.disabled)return;submit.disabled=true;submit.textContent="Encrypting locally…";error.textContent="";try{const[loaded,files,facts,suggestions,processing]=await Promise.all([getCase(caseId),getFilesForCase(caseId),getFactsForCase(caseId),getSuggestionsForCase(caseId),getProcessingForCase(caseId)]),text=await createEncryptedCaseBackup({record:loaded,files,facts,suggestions,processing},password.value);downloadEncryptedCaseBackup(text,record.title);await markCaseBackedUp(caseId);close()}catch(cause){error.textContent=cause instanceof Error?cause.message:"The backup could not be created."}finally{submit.textContent="Encrypt and download";validate()}}}
