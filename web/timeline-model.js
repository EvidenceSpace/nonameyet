function validDate(value){if(!/^\d{4}-\d{2}-\d{2}$/.test(value))return false;const[year,month,day]=value.split("-").map(Number),date=new Date(Date.UTC(year,month-1,day));return date.getUTCFullYear()===year&&date.getUTCMonth()===month-1&&date.getUTCDate()===day}
export function validateTimelineEvent(input){const value={eventDate:String(input.eventDate||"").trim(),title:String(input.title||"").trim(),description:String(input.description||"").trim(),sourceFileId:String(input.sourceFileId||"").trim()};if(!validDate(value.eventDate))return{ok:false,error:"Enter a valid event date."};if(value.title.length<3||value.title.length>120)return{ok:false,error:"Event title must be between 3 and 120 characters."};if(value.description.length>1000)return{ok:false,error:"Event details must be 1,000 characters or fewer."};if(!value.sourceFileId)return{ok:false,error:"Choose the original record that supports this event."};return{ok:true,value}}
export function createTimelineEvent(caseId,input,file,at=new Date().toISOString(),id=`event_${crypto.randomUUID()}`){const result=validateTimelineEvent(input);if(!result.ok)return result;if(!file||file.id!==result.value.sourceFileId||!/^([a-f0-9]{64})$/.test(file.sha256||""))return{ok:false,error:"The selected source record is missing or has no valid SHA-256 hash."};return{ok:true,value:{id,caseId,...result.value,sourceReference:{fileId:file.id,sha256:file.sha256,locator:{kind:"whole_file"}},origin:"user_entered",createdAt:at,updatedAt:at}}}
export function sortTimelineEvents(events){return[...events].sort((a,b)=>String(a.eventDate).localeCompare(String(b.eventDate))||String(a.createdAt).localeCompare(String(b.createdAt))||String(a.id).localeCompare(String(b.id)))}
