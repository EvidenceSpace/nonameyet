import{deleteEvent,getEventsForCase,getFilesForCase,saveEvent}from"./storage.js";import{createTimelineEvent,sortTimelineEvents,timelineLocatorLabel,timelineSourceStatus,updateTimelineEvent}from"./timeline-model.js";
const caseId=new URLSearchParams(location.search).get("id"),checklist=document.querySelector("#checklist");
if(caseId&&checklist){
const section=document.createElement("section");section.className="workspace-card";section.id="timeline";section.innerHTML='<div class="card-heading"><div><span class="section-label">SOURCE-LINKED TIMELINE</span><h3>Events you entered from original records</h3><p>Dates are not inferred or authenticated. Every event stays linked to the exact record hash and optional location you choose.</p><p class="timeline-status" role="status"></p></div><button class="button-secondary" id="add-timeline-event" type="button">Add event</button></div><div class="timeline-list" id="timeline-list"></div>';checklist.before(section);
const nav=document.querySelector(".workspace-nav");if(nav){const link=document.createElement("a");link.href="#timeline";link.innerHTML='<span>◇</span> Timeline <b id="nav-event-count">0</b>';nav.querySelector('a[href="#checklist"]').before(link)}
const dialog=document.createElement("dialog");dialog.className="timeline-dialog";dialog.innerHTML='<form id="timeline-form"><div class="dialog-header"><div><span class="section-label">MANUAL TIMELINE EVENT</span><h2 id="timeline-dialog-title">Add a source-linked event</h2></div><button class="dialog-close" type="button" aria-label="Close">×</button></div><p class="timeline-intro">Enter only what the selected original record supports. CaseFind does not verify that an event occurred.</p><label>Event date<input id="timeline-date" type="date" required></label><label>Event title<input id="timeline-title" maxlength="120" required placeholder="e.g. Final files delivered"></label><label>What does the record show? <span>optional</span><textarea id="timeline-description" maxlength="1000" rows="3"></textarea></label><div class="timeline-source-fields"><label>Supporting original record<select id="timeline-source" required></select></label><label>PDF page <span>optional</span><input id="timeline-page" type="number" min="1" max="10000" step="1" inputmode="numeric"></label></div><label>Supporting excerpt <span>optional, enter exactly as shown</span><textarea id="timeline-quote" maxlength="500" rows="3"></textarea></label><p class="timeline-help">A page number is accepted only for PDF sources. The saved SHA-256 hash identifies the exact local record version.</p><p class="timeline-error" role="status"></p><div class="timeline-actions"><button class="button-secondary timeline-cancel" type="button">Cancel</button><button class="button" type="submit">Save event</button></div></form>';document.body.append(dialog);
const root=section.querySelector("#timeline-list"),status=section.querySelector(".timeline-status"),form=dialog.querySelector("form"),date=dialog.querySelector("#timeline-date"),title=dialog.querySelector("#timeline-title"),description=dialog.querySelector("#timeline-description"),source=dialog.querySelector("#timeline-source"),page=dialog.querySelector("#timeline-page"),quote=dialog.querySelector("#timeline-quote"),error=dialog.querySelector(".timeline-error"),submit=dialog.querySelector('button[type="submit"]'),dialogTitle=dialog.querySelector("#timeline-dialog-title");let events=sortTimelineEvents(await getEventsForCase(caseId)),files=await getFilesForCase(caseId),editing=null;
function escape(value=""){const node=document.createElement("span");node.textContent=value;return node.innerHTML}function dateLabel(value){const[year,month,day]=value.split("-").map(Number);return new Intl.DateTimeFormat(undefined,{day:"numeric",month:"short",year:"numeric",timeZone:"UTC"}).format(new Date(Date.UTC(year,month-1,day)))}function setStatus(message="",kind=""){status.textContent=message;status.className=`timeline-status${kind?` ${kind}`:""}`}
function openSource(item,sourceState){const preview=[...document.querySelectorAll(".preview-file")].find(button=>button.dataset.id===sourceState.file.id);if(!preview)return;preview.click();const locator=item.sourceReference?.locator||{},embed=document.querySelector("#record-preview embed");if(embed&&locator.kind==="pdf_page"&&Number.isInteger(locator.page)){const base=embed.getAttribute("src").split("#")[0];embed.setAttribute("src",`${base}#page=${locator.page}`)}const excerpt=document.querySelector("#source-excerpt"),sourceQuote=document.querySelector("#source-quote");if(excerpt&&sourceQuote&&locator.quote){sourceQuote.textContent=locator.quote;excerpt.hidden=false}}
function render(){document.querySelector("#nav-event-count")?.replaceChildren(String(events.length));if(!events.length){root.innerHTML='<div class="timeline-empty">No timeline events yet. Add events manually from records you have reviewed.</div>';return}root.innerHTML=events.map(event=>{const sourceState=timelineSourceStatus(event,files),locator=timelineLocatorLabel(event.sourceReference?.locator),sourceName=sourceState.file?.name||"Missing record",sourceClass=sourceState.ok?"":" invalid",excerpt=event.sourceReference?.locator?.quote;return`<article class="timeline-event" data-id="${escape(event.id)}"><time class="timeline-date" datetime="${escape(event.eventDate)}">${escape(dateLabel(event.eventDate))}</time><span class="timeline-marker"></span><div class="timeline-copy"><strong>${escape(event.title)}</strong>${event.description?`<p>${escape(event.description)}</p>`:""}<small>User-entered · Source: ${escape(sourceName)} · ${escape(locator)} <span class="timeline-source-state${sourceClass}">${escape(sourceState.label)}</span></small>${excerpt?`<blockquote class="source-quote">${escape(excerpt)}</blockquote>`:""}<details class="timeline-provenance"><summary>Source provenance</summary><p>${escape(sourceName)} · ${escape(locator)}</p><code>SHA-256 ${escape(event.sourceReference?.sha256||"Unavailable")}</code></details><div class="timeline-event-actions">${sourceState.ok?`<button class="timeline-open" type="button">Open source</button>`:""}<button class="timeline-edit" type="button">Edit</button><button class="timeline-remove" type="button">Remove</button></div><p class="timeline-event-status" role="status"></p></div></article>`}).join("");
root.querySelectorAll(".timeline-event").forEach(card=>{const item=events.find(event=>event.id===card.dataset.id),sourceState=timelineSourceStatus(item,files);card.querySelector(".timeline-open")?.addEventListener("click",()=>openSource(item,sourceState));card.querySelector(".timeline-edit").addEventListener("click",()=>open(item));const remove=card.querySelector(".timeline-remove");let timer;remove.onclick=async()=>{if(!remove.classList.contains("armed")){remove.classList.add("armed");remove.textContent="Confirm remove";timer=setTimeout(()=>{remove.classList.remove("armed");remove.textContent="Remove"},5000);return}clearTimeout(timer);remove.disabled=true;try{await deleteEvent(item.id);events=events.filter(event=>event.id!==item.id);setStatus("Timeline event removed locally.");render()}catch{remove.disabled=false;remove.classList.remove("armed");remove.textContent="Remove";card.querySelector(".timeline-event-status").textContent="The timeline event could not be removed. Nothing was changed. Try again.";setStatus("The timeline event could not be removed. Nothing was changed. Try again.","error")}}})}
async function open(item=null){files=await getFilesForCase(caseId);editing=item;form.reset();error.textContent="";source.innerHTML=files.length?files.map(file=>`<option value="${escape(file.id)}">${escape(file.name)}</option>`).join(""):'<option value="">Add an original record first</option>';submit.disabled=files.length===0;date.max="9999-12-31";dialogTitle.textContent=item?"Edit source-linked event":"Add a source-linked event";submit.textContent=item?"Save changes":"Save event";if(item){date.value=item.eventDate;title.value=item.title;description.value=item.description||"";source.value=item.sourceFileId;page.value=item.sourceReference?.locator?.kind==="pdf_page"?item.sourceReference.locator.page:"";quote.value=item.sourceReference?.locator?.quote||""}dialog.showModal();date.focus()}
function close(){dialog.close();form.reset();error.textContent="";editing=null}section.querySelector("#add-timeline-event").onclick=()=>open();dialog.querySelector(".dialog-close").onclick=close;dialog.querySelector(".timeline-cancel").onclick=close;dialog.addEventListener("click",event=>{if(event.target===dialog)close()});
form.onsubmit=async event=>{event.preventDefault();const current=editing,file=files.find(item=>item.id===source.value),input={eventDate:date.value,title:title.value,description:description.value,sourceFileId:source.value,sourcePage:page.value,sourceQuote:quote.value},result=current?updateTimelineEvent(current,input,file):createTimelineEvent(caseId,input,file);if(!result.ok){error.textContent=result.error;return}submit.disabled=true;submit.textContent=current?"Saving changes locally…":"Saving locally…";try{await saveEvent(result.value);events=sortTimelineEvents(current?events.map(item=>item.id===result.value.id?result.value:item):[...events,result.value]);close();setStatus(current?"Timeline event updated locally.":"Timeline event saved locally.");render()}catch{error.textContent="The timeline event could not be saved in this browser. Nothing was changed. Your draft is still open. Try again."}finally{submit.disabled=files.length===0;submit.textContent=current?"Save changes":"Save event"}};
document.querySelector("#file-list").addEventListener("click",event=>{const button=event.target.closest(".delete-file");if(!button||!events.some(item=>item.sourceFileId===button.dataset.id))return;event.preventDefault();event.stopImmediatePropagation();const message=document.querySelector("#upload-message");message.className="upload-message error";message.textContent="Remove or move timeline events linked to this source record before deleting it."},true);render()}
