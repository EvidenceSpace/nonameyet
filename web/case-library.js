import{getAllCases,getFilesForCase,getFactsForCase,getSuggestionsForCase}from"./storage.js";import{summarizeLocalCase,sortLocalCases,filterLocalCases,formatLocalBytes}from"./case-library-model.js";const grid=document.querySelector("#case-grid"),empty=document.querySelector("#library-empty"),noResults=document.querySelector("#library-no-results"),controls=document.querySelector("#library-controls"),search=document.querySelector("#case-search");let items=[];function escape(value=""){const node=document.createElement("span");node.textContent=value;return node.innerHTML}function relativeDate(value){const time=Date.parse(value);if(!Number.isFinite(time))return"Date unavailable";const days=Math.max(0,Math.floor((Date.now()-time)/86400000));if(days===0)return"Updated today";if(days===1)return"Updated yesterday";if(days<30)return`Updated ${days} days ago`;return`Updated ${new Date(time).toLocaleDateString(undefined,{day:"numeric",month:"short",year:"numeric"})}`}function render(){const visible=filterLocalCases(items,search.value);empty.hidden=items.length!==0;controls.hidden=items.length===0;noResults.hidden=items.length===0||visible.length!==0;grid.hidden=visible.length===0;grid.innerHTML=visible.map(item=>`<article class="local-case-card"><div class="local-case-top"><span class="status"><i></i>${escape(item.record.status||"collecting")}</span><small>${relativeDate(item.updatedAt)}</small></div><h2>${escape(item.record.title||"Untitled case")}</h2><p class="local-client">${escape(item.record.client||"No client entered")}</p><div class="local-case-stats"><div><strong>${item.fileCount}</strong><span>records</span></div><div><strong>${item.verifiedFacts}</strong><span>verified facts</span></div><div><strong>${item.pendingReview}</strong><span>to review</span></div></div><div class="local-case-foot"><span>${formatLocalBytes(item.totalBytes)} stored</span><a href="case.html?id=${encodeURIComponent(item.record.id)}">Open case <b>→</b></a></div></article>`).join("")}async function load(){try{const records=await getAllCases(),summaries=await Promise.all(records.map(async record=>{const[files,facts,suggestions]=await Promise.all([getFilesForCase(record.id),getFactsForCase(record.id),getSuggestionsForCase(record.id)]);return summarizeLocalCase(record,files,facts,suggestions)}));items=sortLocalCases(summaries);document.querySelector("#library-count").textContent=items.length;document.querySelector("#library-bytes").textContent=formatLocalBytes(items.reduce((sum,item)=>sum+item.totalBytes,0));render()}catch{grid.innerHTML='<div class="library-load-error"><h2>We couldn’t open local storage.</h2><p>Check that browser storage is available, then reload this page.</p></div>'}}search.addEventListener("input",render);load();
