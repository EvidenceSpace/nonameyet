import { getFilesForCase, getProcessingForCase, getSuggestionsForCase, saveProcessing, saveSuggestion } from "./storage.js";
import { processPdf } from "./pdf-processing.js";
import { requestAnalysis } from "./analysis-client.js";
import { buildGroundedSuggestions } from "./suggestion-handoff.js";
const caseId=new URLSearchParams(location.search).get("id"),root=document.querySelector("#file-list");let refreshing=false;const labels={extracting:"Extracting…",ready_for_ai:"Text ready",needs_ocr:"OCR needed",failed:"Failed"};
const viewer=document.createElement("dialog");viewer.className="text-source-dialog";viewer.innerHTML=`<div class="text-source-head"><div><span class="section-label">EXTRACTED SOURCE TEXT</span><h2 id="text-source-title"></h2></div><button class="dialog-close" id="close-text-source" type="button" aria-label="Close">×</button></div><div class="text-source-trust">Local extraction only · Review the original record before confirming any fact.</div><div class="text-source-meta"><span id="text-source-page"></span><span id="text-source-adapter"></span></div><pre id="text-source-content"></pre><div class="text-source-footer"><code id="text-source-hash"></code><div><button class="button-secondary" id="previous-text-page" type="button">Previous</button><button class="button-secondary" id="next-text-page" type="button">Next</button></div></div>`;document.body.append(viewer);viewer.querySelector("#close-text-source").onclick=()=>viewer.close();let viewedPages=[],viewedIndex=0;function renderViewedPage(){const page=viewedPages[viewedIndex];viewer.querySelector("#text-source-page").textContent=`Page ${page.pageNumber} of ${viewedPages.length}`;viewer.querySelector("#text-source-content").textContent=page.text||"No selectable text on this page.";viewer.querySelector("#previous-text-page").disabled=viewedIndex===0;viewer.querySelector("#next-text-page").disabled=viewedIndex===viewedPages.length-1}viewer.querySelector("#previous-text-page").onclick=()=>{if(viewedIndex>0){viewedIndex--;renderViewedPage()}};viewer.querySelector("#next-text-page").onclick=()=>{if(viewedIndex<viewedPages.length-1){viewedIndex++;renderViewedPage()}};function openTextSource(file,job){viewedPages=job.artifact?.pages||[];if(!viewedPages.length)return;viewedIndex=0;viewer.querySelector("#text-source-title").textContent=file.name;viewer.querySelector("#text-source-adapter").textContent=job.artifact.adapterVersion;viewer.querySelector("#text-source-hash").textContent=`SHA-256 ${job.fileHash}`;renderViewedPage();viewer.showModal()}
async function refresh(){if(!caseId||!root||refreshing)return;refreshing=true;try{const[files,jobs]=await Promise.all([getFilesForCase(caseId),getProcessingForCase(caseId)]);[...root.querySelectorAll(".file-row")].forEach((row,index)=>{const file=files[index];if(!file)return;const actions=row.querySelector(".file-actions");if(!actions)return;const job=jobs.find(item=>item.fileId===file.id),status=job?.status||(file.type==="application/pdf"?"unprocessed":"needs_ocr");let badge=actions.querySelector(".processing-status");if(!badge){badge=document.createElement("span");actions.prepend(badge)}badge.className=`processing-status ${status}`;badge.textContent=labels[status]||"Not processed";let processButton=actions.querySelector(".process-file");const canProcess=file.type==="application/pdf"&&(!job||job.status==="failed");if(canProcess&&!processButton){processButton=document.createElement("button");processButton.type="button";processButton.className="process-file";actions.insertBefore(processButton,badge.nextSibling);processButton.onclick=()=>run(file)}if(processButton){if(canProcess)processButton.textContent=job?"Retry":"Extract text";else processButton.remove()}let viewButton=actions.querySelector(".view-extracted-text");if(job?.status==="ready_for_ai"&&job.artifact?.pages?.length){if(!viewButton){viewButton=document.createElement("button");viewButton.type="button";viewButton.className="view-extracted-text";actions.insertBefore(viewButton,actions.querySelector(".preview-file"))}viewButton.textContent="View text";viewButton.onclick=()=>openTextSource(file,job);let analyzeButton=actions.querySelector(".analyze-record");if(!analyzeButton){analyzeButton=document.createElement("button");analyzeButton.type="button";analyzeButton.className="analyze-record";actions.insertBefore(analyzeButton,viewButton)}analyzeButton.textContent="Find details";analyzeButton.onclick=()=>analyze(file,job,row,analyzeButton)}else{viewButton?.remove();actions.querySelector(".analyze-record")?.remove()}let note=row.querySelector(".processing-note");if(job?.message){if(!note){note=document.createElement("p");note.className="processing-note";row.append(note)}note.textContent=job.message}else note?.remove()})}finally{refreshing=false}}
async function analyze(file,job,row,button){const consent=confirm(`Send extracted text from “${file.name}” to the configured AI provider?\n\nThe original file stays on this device. AI results are suggestions only and must be reviewed.`);if(!consent)return;button.disabled=true;button.textContent="Analyzing…";let note=row.querySelector(".processing-note");try{const candidates=await requestAnalysis({file,processingJob:job,consentAccepted:true});const grounded=buildGroundedSuggestions({caseId,file,processingJob:job,candidates});const existing=await getSuggestionsForCase(caseId);const fingerprints=new Set(existing.map(item=>`${item.fileId}\u0000${item.label}\u0000${item.value}\u0000${item.sourceReference?.locator?.quote||""}`));let saved=0;for(const item of grounded){const key=`${item.fileId}\u0000${item.label}\u0000${item.value}\u0000${item.sourceReference.locator.quote}`;if(!fingerprints.has(key)){await saveSuggestion(item);saved++}}if(saved)location.reload();else{note.textContent="No new source-grounded details were found.";button.disabled=false;button.textContent="Find details"}}catch(error){note.textContent=error?.code==="analysis_unavailable"?"AI analysis is not configured for this preview.":error?.code==="rate_limited"?"Analysis limit reached. Wait a moment and try again.":"Analysis failed safely. No suggestions were saved.";button.disabled=false;button.textContent="Retry analysis"}}
async function run(file){await saveProcessing({fileId:file.id,caseId,fileHash:file.sha256,status:"extracting",message:"Reading selectable text locally…",updatedAt:new Date().toISOString()});await refresh();await saveProcessing(await processPdf(file));await refresh()}if(root){new MutationObserver(()=>queueMicrotask(refresh)).observe(root,{childList:true,subtree:true});refresh()}
