import { createId,deleteCase,deleteFile,getCase,getFilesForCase,saveCase,saveFile,sha256 } from "./storage.js";const params=new URLSearchParams(location.search),caseId=params.get("id"),workspace=document.querySelector("#workspace"),errorView=document.querySelector("#workspace-error"),fileInput=document.querySelector("#file-input"),message=document.querySelector("#upload-message"),checklistItems=[["agreement","Project agreement or proposal","Scope, price, or original terms"],["price","Agreed price and payment schedule","Messages or documents showing the amount"],["invoice","Invoice","The amount requested and payment details"],["delivery","Proof the work was delivered","Email, link, or transfer confirmation"],["approval","Client approval or feedback","Acceptance, review, or change requests"],["reminders","Payment reminders and responses","Follow-ups and the client’s replies"]];let record,files=[];
function formatBytes(bytes){if(!bytes)return"0 bytes";const units=["bytes","KB","MB"],index=Math.min(Math.floor(Math.log(bytes)/Math.log(1024)),2);return`${(bytes/1024**index).toFixed(index?1:0)} ${units[index]}`}
function escapeHtml(value){const node=document.createElement("span");node.textContent=value;return node.innerHTML}
function renderChecklist(){const root=document.querySelector("#workspace-checklist");root.innerHTML=checklistItems.map(([key,title,description])=>`<label class="checklist-row"><input type="checkbox" data-key="${key}" ${record.checklist?.includes(key)?"checked":""}/><span class="check-box"></span><span><strong>${title}</strong><small>${description}</small></span></label>`).join("");root.querySelectorAll("input").forEach(input=>input.addEventListener("change",async()=>{record.checklist=[...root.querySelectorAll("input:checked")].map(item=>item.dataset.key);record.updatedAt=new Date().toISOString();await saveCase(record);renderStats()}))}
function renderStats(){document.querySelector("#file-count").textContent=String(files.length);document.querySelector("#nav-file-count").textContent=String(files.length);document.querySelector("#file-size").textContent=formatBytes(files.reduce((sum,file)=>sum+file.size,0));document.querySelector("#check-progress").textContent=`${record.checklist?.length||0}/6`}
function renderFiles(){const root=document.querySelector("#file-list");if(!files.length){root.innerHTML='<div class="empty-files">No records added yet. Start with an agreement, invoice, or delivery message.</div>';renderStats();return}root.innerHTML=files.sort((a,b)=>a.createdAt.localeCompare(b.createdAt)).map(file=>`<div class="file-row"><span class="file-type">${file.type==="application/pdf"?"PDF":"IMG"}</span><div class="file-copy"><strong title="${escapeHtml(file.name)}">${escapeHtml(file.name)}</strong><small>${formatBytes(file.size)} · SHA-256 ${file.sha256.slice(0,10)}…</small></div><div class="file-actions"><span class="stored-label">Stored locally</span><button class="delete-file" data-id="${file.id}" type="button">Remove</button></div></div>`).join("");root.querySelectorAll(".delete-file").forEach(button=>button.addEventListener("click",async()=>{await deleteFile(button.dataset.id);files=files.filter(file=>file.id!==button.dataset.id);renderFiles()}));renderStats()}
async function addFiles(selected){message.className="upload-message";message.textContent="";for(const file of selected){if(!["image/png","image/jpeg","image/heic","application/pdf"].includes(file.type)){message.className="upload-message error";message.textContent=`${file.name} was skipped: use PNG, JPEG, HEIC, or PDF.`;continue}if(file.size>20*1024*1024){message.className="upload-message error";message.textContent=`${file.name} was skipped: the 20 MB limit was exceeded.`;continue}if(files.length>=100){message.className="upload-message error";message.textContent="This preview supports up to 100 files per case.";break}message.textContent=`Checking ${file.name}…`;const hash=await sha256(file);if(files.some(existing=>existing.sha256===hash)){message.className="upload-message error";message.textContent=`${file.name} is already in this case.`;continue}const stored={id:createId("file"),caseId,name:file.name,type:file.type,size:file.size,sha256:hash,createdAt:new Date().toISOString(),original:file};try{await saveFile(stored);files.push(stored);message.textContent=`Stored ${file.name} locally.`}catch(err){message.className="upload-message error";message.textContent=err?.name==="ConstraintError"?`${file.name} is already in this case.`:"The file could not be stored."}}fileInput.value="";renderFiles()}
async function init(){if(!caseId){errorView.hidden=false;return}record=await getCase(caseId);if(!record){errorView.hidden=false;return}files=await getFilesForCase(caseId);document.querySelector("#sidebar-title").textContent=record.title;document.querySelector("#workspace-title").textContent=record.title;document.querySelector("#workspace-summary").textContent=record.summary;document.querySelector("#workspace-amount").textContent=record.amount?`₹${record.amount}`:"Not entered";document.title=`${record.title} — CaseFind`;renderChecklist();renderFiles();workspace.hidden=false}fileInput.addEventListener("change",()=>addFiles([...fileInput.files]));const dropzone=document.querySelector("#dropzone");["dragenter","dragover"].forEach(type=>dropzone.addEventListener(type,event=>{event.preventDefault();dropzone.classList.add("dragging")}));["dragleave","drop"].forEach(type=>dropzone.addEventListener(type,event=>{event.preventDefault();dropzone.classList.remove("dragging")}));dropzone.addEventListener("drop",event=>addFiles([...event.dataTransfer.files]));const dialog=document.querySelector("#delete-dialog");document.querySelector("#delete-case").addEventListener("click",()=>dialog.showModal());document.querySelector("#confirm-delete").addEventListener("click",async event=>{event.preventDefault();await deleteCase(caseId);location.href="index.html"});init().catch(()=>{errorView.hidden=false});
